[project]
name = "calculator"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["calculator_backend"]

[project.scripts]
calculator = "calculator.cli:cli"

[tool.uv.sources]
calculator_backend = { workspace = true }
